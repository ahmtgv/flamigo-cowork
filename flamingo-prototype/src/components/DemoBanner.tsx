export default function DemoBanner() {
  return (
    <div className="demo-banner" role="note">
      DEMO — тестовый прототип Flamingo. Данные синтетические, камера не записывает и ничего не отправляет.
    </div>
  );
}
